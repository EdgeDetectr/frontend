import React from "react";
import { Typography } from "@mui/material";

const Heading = () => {
  return (
    <div>
      <Typography variant="h3">edge detectr</Typography>
      <Typography variant="h6">free edge detector.</Typography>
      <Typography variant="h6">all operators.</Typography>
      <Typography variant="h6">all image formats.</Typography>
    </div>
  );
};

export default Heading;
